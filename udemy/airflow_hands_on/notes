-- install astro cli with docker as airflow backend
brew install astronomer/tap/astro --without-podman

-- if astronomer is not in brew tap, add it using
brew tap astronomer/tap

-- check if astro is succesfully installed
astro version

-- initialize astro project
astro dev init

-- init with specific runtime, example:
astro dev init --runtime-version 12.9.0

-- start airflow and containers
astro dev start

-- default credentials for airflow web
admin/admin

-- if default port 5432 conflicts with local postgres instance, change
-- docker postgres port:
astro config set postgres.port 5433

-- access the airflow cli (inside webserver instance) using astro, must
-- be inside airflow dir
astro dev bash

-- check if metadata db can be reached
airflow db check

-- purge all records in db tables and move them in archive tables
airflow db clean

-- export archived data from archive tables in csv format
airflow db export-archived

-- remove archive data from db
airflow db drop archive

-- run subsections of a dag for a specified data range
airflow dags backfill my_dag --reset-dagrun --rerun-failed-tasks --run-backwards \
-s 2024-01-01 -e 2024-01-10

--resync dags (to show in ui)
airflow dags reserialize

-- list all dags (to check if airflow recognizes specific dags)
airflow dags list

-- test a task instance
airflow tasks test my_dag my_task 2024-01-01

-- list all available commands
airflow cheat-sheet

-- To stop the service and prevent automatic startup:
-- Identify the PostgreSQL launch agent file, typically 
-- found in /Library/LaunchDaemons or /Library/LaunchAgents.
-- You can search for it using:
sudo find /Library/LaunchDaemons /Library/LaunchAgents -name "*postgres*"

-- If a .plist file (e.g., postgresql-16.plist) is found, unload it to stop 
-- the service and prevent it from launching at system startup:
sudo launchctl unload -w /Library/LaunchDaemons/postgresql-16.plist

-- (Replace postgresql-16.plist with the actual filename found.) Verify that PostgreSQL is disabled.
sudo launchctl list | grep postgres

-- to restart astro environment (such as when adding new modules in requirements.txt)
astro dev restart

-- build Spark image that will run the Spark jobs
cd spark/notebooks/spark-transform
docker build -t airflow/stock-app .

-- run backfill on a dag using cli
airflow dags backfill -s <start> -e <end> dag_id

-- Airflow Schedules: CRON vs timedelta 
CRON - airflow assumes you will always run your DAG 
at a specific time
    - respects DST

Timedelta - airflow assumes you will always run your
DAG at a specific interval
    - doesn't respect DST

-- Datasets - alternative to Sensors (less resources intensive), sets dependencies between dags based on file update events, the underlying data in the file is not relevant
    - can set file/s as datasets, when producer 
    dataset runs successful then consumer datasets
    will be run
    - unsuccessful runs will not trigger consumer datasets
    - datasets can be schedule based on AND/OR logic
    - to run dags based on datasets and time-based schedule use the DatasetOrTimeSchedule class

-- use .airflowignore file so that the scheduler will not parse those files in the dags folder

-- addtional dag parameters: on_success_callback, on_failure_callback
-- assign a function that will handle what to do depending on the outcome

-- task callbacks: on_success_callback, on_failure_callback, on_skipped_callback, on_retry_callback,
-- on_execute_callback

-- defaul_args:
retries - how many times to run task before failure
retry_delay - delay between each retry
retry_exponential_backoff - applies exponential retry delay after each failed attempt
max_retry_delay - max allowed limit of retry delay until task is considered failed